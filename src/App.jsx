// PromptForge v3 — paste the full artifact here.
// All fetch calls must target: /.netlify/functions/generate-prompt
// Replace this placeholder with the v3 artifact when provided.

export default function App() {
  return (
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', fontFamily: "'DM Sans', sans-serif", color: '#e2e8f0' }}>
      <p style={{ color: '#64748b' }}>PromptForge — paste v3 artifact into this file.</p>
    </div>
  )
}
